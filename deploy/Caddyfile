{$BUHTA_DOMAIN} {
	encode gzip zstd

	@api path /api/* /health /account/password /analytics* /auth/me /catalog* /clients* /courier* /distributor* /notifications* /operations* /production* /users*
	reverse_proxy @api api:3000

	reverse_proxy web:3001
}
