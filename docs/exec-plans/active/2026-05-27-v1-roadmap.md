# V1 Roadmap: Полная CRM «Бухта»

Статус: `Active / Meta-plan`
Дата: 2026-05-27
Источник: `gstack-plan-eng-review` + последующее product/engineering обсуждение.

## Summary

Этот документ — долгоживущая дорожная карта v1, а не детальный execution plan одного этапа. Он всегда остается активным, пока v1 не завершена.

Реализуем полную v1 последовательно и основательно, без частичного production-запуска пользователям. Пользовательское тестирование начинается после сборки цельного продукта, затем идут правки, полировка и уточнение сценариев.

При этом внутри разработки каждый этап закрывается инженерным checkpoint:

- backend tests;
- integration tests на real Postgres для критичных операций;
- docs check;
- локальная ручная проверка UI владельцем проекта;
- исправление найденных проблем до перехода к следующему крупному этапу.

Основная последовательность: роли/админка и mobile/PWA shell → shared contracts/domain core → справочники → производство → перемещения → распределитель/курьер → продажи → сгрузка/списания → отчеты/audit.

Архитектурный принцип: append-only операции + защищенные текущие balance projections, обновляемые в одной Prisma transaction.

Под каждый крупный этап этой roadmap создается отдельный execution plan в `docs/exec-plans/active/`. Такой план должен описывать конкретные решения, scope, файлы, миграции, тесты, риски и критерии завершения этапа. После завершения этапа его plan переносится в `docs/exec-plans/completed/`, а эта roadmap обновляется по фактическому прогрессу.

Принятые решения:
- Source of truth: `Operation`/audit envelope + typed operation details + transactional balance tables.
- Concurrency: conditional update + `idempotencyKey` для критичных write-команд.
- Права задаются через policy layer; текущая policy для дисконта, отмены и корректировок: `director` и `admin`, с возможностью быстро добавить роли позже.
- Backend modules: по доменным зонам, не по таблицам.
- Contracts: `packages/shared` zod schemas + inferred TS types.
- Errors: единый `{ code, message, details }` contract.
- Tests: real Postgres integration tests per phase + unit tests.
- PWA/mobile foundation начинается рано: app-like shell, role home screens, mobile navigation, cache только shell/static/read-only data; write-операции online-only.

## Roadmap Phases

### 0. Project Structure And Architecture Baseline

- Статус: `Completed` в плане `docs/exec-plans/completed/2026-05-28-architecture-baseline-and-domain-core.md`.
- Зафиксирована структура папок `apps/web`, `apps/api`, `packages/shared`.
- Уточнена архитектурная карта модулей в `docs/ARCHITECTURE.md`.
- Зафиксированы shared contracts, backend modules, policy layer, operation/audit/idempotency baseline.
- Добавлены real Postgres integration tests и HTTP BetterAuth cookie -> Actor -> PolicyGuard test.
- Уточнены testing harness, smoke и sandbox/tooling notes в `docs/DEVELOPMENT.md`.

Отдельный execution plan нужен, если меняется структура приложения, границы модулей, Prisma baseline или общий verification contour.

1. **Roles, Admin, Policy Foundation**
   - Статус: частично выполнено в первом кодовом этапе.
   - Заменить spike-style role guard на policy registry/guard.
   - Зафиксировать роли: `admin`, `director`, `production_manager`, `commercial_manager`, `distributor_worker`, `courier`.
   - Добавить admin user management, seed пользователей/ролей, protected routes.
   - Целевой вход в v1: администратор создает пользователя, назначает роль, генерирует логин и временный пароль; самостоятельной регистрации и обязательного email в UI нет.
   - Все permission checks проходят backend policy layer, не frontend.
   - Policy rules должны меняться в одном месте, без поиска проверок по controllers/UI.

2. **Mobile/PWA Foundation**
   - Завершенный детальный план: `docs/exec-plans/completed/2026-05-28-admin-login-and-mobile-shell.md`.
   - Создать mobile-first app shell.
   - Добавить PWA manifest, базовые app icons, standalone display mode, theme/status colors.
   - Добавить role home screen pattern для ежедневных операций.
   - Добавить mobile navigation pattern для операционных ролей.
   - Зафиксировать explicit offline write blocking: при отсутствии соединения write-действия недоступны или возвращают понятную ошибку.
   - Не реализовывать offline queue, background sync и разрешение конфликтов остатков в v1.

3. **Shared Contracts And Domain Core**
   - Статус: baseline выполнен в первом кодовом этапе.
   - В `packages/shared` добавить zod contracts для commands/responses/errors.
   - Ввести value helpers для денег в копейках и количеств с unit.
   - В API добавить domain error mapper в HTTP responses.
   - В Prisma добавить базовые модели для append-only operation envelope, audit log, idempotency records и balance projections.
   - Для важных операций добавить typed details вместо одного универсального JSON payload.

4. **Catalog And Admin Data**
   - Статус: completed в плане `docs/exec-plans/completed/2026-05-28-catalog-foundation.md`.
   - Справочники: виды сырья, тара/упаковка, шаблоны продукции, распределители.
   - CRUD здесь допустим, но все изменения проходят role/policy checks и audit.
   - Product template changes не меняют уже выпущенные партии.
   - Кодовый контур справочников добавлен в Prisma/shared/API/web и закрыт полным verification contour.

5. **Production Flow**
   - Статус: production baseline completed в плане `docs/exec-plans/completed/2026-05-29-production-intake-and-release.md`.
   - Реализовано: цена шаблона продукции, поступление сырья/тары, балансы цеха, выпуск партии продукции в статусе `in_workshop` со snapshot полей шаблона и цены.
   - Реализовано: списание тары по правилу 1 единица тары на 1 единицу продукции; расход сырья вводится вручную в форме выпуска.
   - Реализовано: перемещение готовой продукции из цеха на распределитель через отдельные balance projections для цеха и распределителя.
   - Реализовано: read-only экран и API товарных остатков распределителя с рублевым товарным балансом.
   - Каждая write-команда: validate → policy → transaction → operation/audit → balance update.

6. **Inventory, Courier, Sales, Cash**
   - Загрузка курьера с распределителя.
   - Продажа с распределителя и продажа курьером.
   - Сгрузка товара и наличных от курьера.
   - Списание наличных директором.
   - Дисконт как заранее назначенный priced stock allocation на конкретное количество остатка, без ручной скидки в момент продажи.
   - Отмена/корректировка только новой операцией со ссылкой на исходную.

7. **Reports, Audit, PWA Completion**
   - Query services для остатков, истории, движения денег/товара и базовой статистики.
   - Pagination/filtering для истории и отчетов.
   - Проверить PWA installability и performance на мобильном viewport.
   - `HANDLER-MAP.md`, `ARCHITECTURE.md`, `SECURITY.md`, `FRONTEND.md`, `DEVELOPMENT.md` обновить по фактическим handlers/tests.

## Current V1 Progress And Remaining Blocks

Статус на 2026-05-31 после этапа Courier Sales And Courier Cash Balance.

Закрытые блоки:

1. Архитектурный baseline и структура monorepo.
2. Better Auth baseline, роли, backend policy layer и operation/audit/idempotency foundation.
3. Admin login, управление пользователями, временные пароли и mobile/PWA shell.
4. Shared contracts/domain core: zod contracts, money helpers, error shape, baseline tests.
5. Catalog foundation: виды сырья, виды тары, распределители, шаблоны продукции, цена шаблона в `priceCents`.
6. Production baseline: приход сырья/тары, балансы цеха, выпуск партии продукции в статусе `in_workshop`, snapshot цены/связанных справочников, списание сырья и тары.
7. Production → Distributor Transfer: workshop product balance, distributor product balance, частичное перемещение, запрет перемещения сверх остатка, operation/audit и mobile форма `На распределитель`.
8. Production frontend baseline: mobile экран заведующего производством, drill-down остатки сырья/тары/продукции, отдельные формы прихода, выпуска и перемещения, icon-only bottom navigation, текущие visual conventions.
9. Distributor Inventory Read Model: `GET /distributor/inventory`, summary товарных остатков и товарного баланса, UI для ролей с `distributor.stock.read`.
10. Clients Foundation: `Client` master data, уникальность нормализованного телефона, `client.read`/`client.manage`, `GET/POST/PATCH /clients`, вкладка `Клиенты`, read-only режим директора и create/update UI для ролей продаж.
11. Sales From Distributor: `DistributorSale`, `DistributorCashBalance`, `GET /distributor/sale-options`, `GET /distributor/cash-balances`, `POST /distributor/sales`, продажа по конкретной строке товарного остатка, cash/cashless поведение, mobile flow продажи с inline созданием клиента.
12. Courier Load And Courier Balance: `CourierProductBalance`, `CourierLoad`, `GET /courier/load-options`, `GET /courier/product-balances`, `POST /courier/loads`, self-load курьера с распределителя, read-only просмотр курьерских товарных балансов для коммерческого руководителя и Директора.
13. Courier Sales And Courier Cash Balance: `CourierSale`, `CourierCashBalance`, `GET /courier/sale-options`, `GET /courier/cash-balances`, `POST /courier/sales`, продажа курьером по конкретной строке собственного товарного остатка, cash/cashless поведение, read-only просмотр наличных балансов курьеров.
14. Courier Unload: `CourierUnload`, `CourierUnloadItem`, `GET /courier/unload-options`, `POST /courier/unloads`, сгрузка нескольких товарных строк и/или наличных курьером на активный распределитель, cash-only/product-only сценарии, запрет сгрузки сверх баланса.
15. Production Notifications: `ProductionNotification`, `GET /notifications`, `POST /notifications`, `PATCH /notifications/:id/complete`, свободные задачи от коммерческого руководителя производству, статусы `new/completed`, HTTP polling без WebSocket/SSE, operation/audit.
16. Director Cash Write-off: `DistributorCashWithdrawal`, `POST /distributor/cash-withdrawals`, списание наличных с активного распределителя Директором или администратором, optional комментарий, запрет списания сверх cash balance, operation/audit.
17. Discounts Priced Stock: `ProductDiscountAssignment`, `POST /distributor/discounts`, priced stock rows с `unitPriceCents` на распределителе и у курьера, перенос effective price через загрузку/возврат, продажи с base/effective/discount snapshot.

Текущая точка продолжения:

- продукция уже может быть выпущена в цеху;
- готовую продукцию можно частично или полностью переместить из цеха на активный распределитель;
- распределитель имеет товарный баланс по партиям и read-only экран/API остатков;
- клиентская база уже поддерживает поиск, создание и редактирование актуальной карточки клиента;
- продажа с распределителя уже уменьшает товарный остаток и увеличивает наличный баланс только при оплате наличными;
- курьер уже может загрузить товар с распределителя на собственный товарный баланс;
- курьер уже может продать товар со своего баланса, а наличная продажа увеличивает его наличный баланс;
- курьер уже может сгрузить несколько товарных строк и/или наличные на выбранный активный распределитель;
- коммерческий руководитель уже может создать свободную задачу производству, а заведующий производством отметить ее выполненной;
- Директор уже может списать наличные с активного распределителя;
- Директор уже может назначить дисконт на конкретное количество товарного остатка распределителя;
- продажи, загрузка курьера и возврат курьера уже сохраняют фактическую цену priced stock row;
- коммерческий руководитель и Директор уже видят read-only товарные и наличные балансы курьеров;
- корректировки и отчеты еще не реализованы.

Рекомендуемый порядок следующих блоков:

1. **Corrections**
    - отмена/корректировка только новой операцией со ссылкой на исходную.
2. **Reports, Audit UI, PWA Polish**
    - отчеты по остаткам, движению товара, движению денег и выручке;
    - UI журнала действий;
    - фильтры/пагинация истории;
    - PWA installability/performance/mobile verification.

Каждый пункт выше является кандидатом на отдельный execution plan. Не начинать следующий крупный пункт без active plan, scope/out-of-scope, схемы данных, API/contracts, frontend flow и verification plan.

## Per-stage Plan Rules

Перед началом каждого этапа нужно создать отдельный plan-файл:

```text
docs/exec-plans/active/YYYY-MM-DD-<stage-name>.md
```

План этапа должен содержать:

- цель этапа;
- scope и out of scope;
- конкретные архитектурные решения этапа;
- затронутые документы;
- затронутые модули и файлы;
- Prisma/schema/migration changes, если есть;
- API/contracts changes, если есть;
- frontend flows и mobile/PWA требования, если есть;
- тестовый план;
- ручную UI-проверку, если есть UI;
- риски и rollback;
- критерии завершения.

После завершения этапа:

- выполнить verification из плана;
- обновить SoR-документы;
- перенести plan в `docs/exec-plans/completed/`;
- обновить эту roadmap, если фактическая последовательность или решения изменились.

## Public Interfaces
- Command handler shape: `execute(actor, command, idempotencyKey)`; actor берется из BetterAuth session.
- Shared error shape: `{ code: string; message: string; details?: unknown }`.
- Critical command responses возвращают operation id, affected balances и user-facing status.
- API не принимает финальные цены/скидки от продавца, кроме выбора заранее разрешенного discounted stock item.
- Read path идет через query services, не через Prisma reads в controllers.
- Operation persistence shape: общий `Operation`/audit envelope для факта действия + typed detail models для продаж, загрузок, сгрузок, выпуска, списаний и корректировок.

## Test Plan
- Unit: money/quantity helpers, policy matrix, discount calculations, allowed status transitions.
- Integration with real Postgres: every command happy path, insufficient stock/cash, insufficient permissions, idempotent retry, audit write, balance before/after.
- Concurrency: parallel sale/load/unload attempts cannot create negative balances or double-spend stock.
- API: anonymous `401`, wrong role `403`, valid role `200/201`, stable error codes.
- Frontend: mobile role flows, offline write disabled/error state, no horizontal overflow, core forms and loading/error states, PWA manifest/installability after PWA foundation.
- Manual UI verification: владелец проекта регулярно проверяет интерфейс руками после готовности каждого крупного flow.
- Required verification per phase: `pnpm lint`, `pnpm lint:boundaries`, `pnpm typecheck`, `pnpm test`, `pnpm docs:check`, `pnpm audit`.

## Assumptions And References
- Полная v1 тестируется целиком после реализации, но каждый этап обязан проходить targeted verification до следующего этапа.
- Частичные production-релизы пользователям до готовности полной v1 не планируются.
- Инженерные checkpoints не являются пользовательскими релизами; они нужны, чтобы не копить ошибки до конца разработки.
- Admin user management — ранняя часть v1, потому что администратор создает пользователей и назначает роли.
- Права ролей могут уточняться по ходу разработки; policy layer должен делать такие изменения быстрыми и локальными.
- Existing modified docs in the worktree are treated as user changes and must not be reverted.
- External checks used: Prisma transactions docs, PostgreSQL row-level locking docs, NestJS guards/authorization docs:
  - https://www.prisma.io/docs/concepts/components/prisma-client/transactions
  - https://www.postgresql.org/docs/17/explicit-locking.html
  - https://docs.nestjs.com/security/authorization
