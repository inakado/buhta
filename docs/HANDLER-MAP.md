# HANDLER-MAP

Карта обработчиков проекта «Бухта».

Статус: `Draft`.

## 1. Назначение

Документ заполняется после появления API и нужен, чтобы быстро видеть:

- HTTP route;
- command/query handler;
- сервис или policy, где проверяются права;
- какие балансы меняются;
- какие события/записи истории создаются;
- какие тесты покрывают поток.

## 2. Текущие handlers

| Route | Handler | Domain operation | Rights | History | Tests |
|---|---|---|---|---|---|
| `GET /health` | `HealthController.health` | runtime health | public via `AllowAnonymous` | нет | `apps/api/test/health.test.ts` |
| `GET /auth/me` | `AuthMeController.me` | current actor/session summary | BetterAuth protected route | нет | `apps/api/test/auth-me.test.ts`, `apps/api/test/auth-http.integration.test.ts` |
| `GET /auth-spike/director-only` | `AuthSpikeController.directorOnly` | protected policy smoke route | `cash.withdraw` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/policy.test.ts`, `apps/api/test/auth-http.integration.test.ts` |
| `POST /account/password` | `AccountController.changeOwnPassword` | authenticated user changes own password after current-password verification; current session remains active | BetterAuth authenticated route with service-level actor build, no `users.manage` required | `operation` + `audit_log` с `user.password.change`, без plaintext password/hash | `apps/api/test/users-controller.test.ts`, `apps/api/test/users-db.integration.test.ts`, `apps/api/test/auth-http.integration.test.ts` |
| `GET /operations/history` | `OperationsController.history` | read operation history from `audit_log` + `operation` with filters, max 90-day range and cursor pagination | `operation.history.read` через `RequirePermission` + `PolicyGuard` | нет, read-only endpoint | `apps/api/test/operation-history-controller.test.ts`, `apps/api/test/operation-history-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /operations/history/options` | `OperationsController.historyOptions` | read filter options for operation history: operation types, roles, actor users and entity types | `operation.history.read` через `RequirePermission` + `PolicyGuard` | нет, read-only endpoint | `apps/api/test/operation-history-controller.test.ts`, `apps/api/test/operation-history-db.integration.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /users` | `UsersController.listUsers` | user list for admin baseline | `users.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/users-controller.test.ts`, `apps/api/test/users-db.integration.test.ts`, `apps/api/test/auth-http.integration.test.ts` |
| `POST /users` | `UsersController.createUser` | admin creates login/password user | `users.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `user.create`, без plaintext password | `apps/api/test/users-controller.test.ts`, `apps/api/test/users-db.integration.test.ts`, `apps/api/test/auth-http.integration.test.ts` |
| `PATCH /users/:userId/role` | `UsersController.updateUserRole` | update user role; self-role update запрещен | `users.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `user.role.update` | `apps/api/test/users-controller.test.ts`, `apps/api/test/users-db.integration.test.ts`, `apps/api/test/auth-http.integration.test.ts` |
| `POST /users/:userId/reset-password` | `UsersController.resetUserPassword` | admin resets temporary password; self-reset запрещен | `users.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `user.password.reset`, без plaintext password | `apps/api/test/auth-http.integration.test.ts`, `apps/api/test/users-db.integration.test.ts` |
| `GET /catalog/raw-material-types` | `CatalogController.listRawMaterialTypes` | list raw material type dictionary | `catalog.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/catalog-controller.test.ts`, `apps/api/test/catalog-db.integration.test.ts` |
| `POST /catalog/raw-material-types` | `CatalogController.createRawMaterialType` | create raw material type | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.raw_material_type.create` | `apps/api/test/catalog-controller.test.ts`, `apps/api/test/catalog-db.integration.test.ts` |
| `PATCH /catalog/raw-material-types/:id` | `CatalogController.updateRawMaterialType` | update raw material type, включая `active` | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.raw_material_type.update` | `apps/api/test/catalog-db.integration.test.ts` |
| `PATCH /catalog/raw-material-types/:id/archive` | `CatalogController.archiveRawMaterialType` | set raw material type `active=false` | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.raw_material_type.archive` | `apps/api/test/catalog-db.integration.test.ts` |
| `GET /catalog/packaging-types` | `CatalogController.listPackagingTypes` | list packaging type dictionary | `catalog.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/catalog-db.integration.test.ts` |
| `POST /catalog/packaging-types` | `CatalogController.createPackagingType` | create packaging type | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.packaging_type.create` | `apps/api/test/catalog-db.integration.test.ts` |
| `PATCH /catalog/packaging-types/:id` | `CatalogController.updatePackagingType` | update packaging type, включая `active` | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.packaging_type.update` | `apps/api/test/catalog-db.integration.test.ts` |
| `PATCH /catalog/packaging-types/:id/archive` | `CatalogController.archivePackagingType` | set packaging type `active=false` | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.packaging_type.archive` | `apps/api/test/catalog-db.integration.test.ts` |
| `GET /catalog/distributors` | `CatalogController.listDistributors` | list distributor dictionary | `catalog.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/catalog-db.integration.test.ts` |
| `POST /catalog/distributors` | `CatalogController.createDistributor` | create distributor | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.distributor.create` | `apps/api/test/catalog-controller.test.ts`, `apps/api/test/catalog-db.integration.test.ts` |
| `PATCH /catalog/distributors/:id` | `CatalogController.updateDistributor` | update distributor, включая `active` | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.distributor.update` | `apps/api/test/catalog-db.integration.test.ts` |
| `PATCH /catalog/distributors/:id/archive` | `CatalogController.archiveDistributor` | set distributor `active=false` | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.distributor.archive` | `apps/api/test/catalog-db.integration.test.ts` |
| `GET /catalog/product-templates` | `CatalogController.listProductTemplates` | list product template dictionary with raw/packaging links and `priceCents` | `catalog.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/catalog-db.integration.test.ts` |
| `POST /catalog/product-templates` | `CatalogController.createProductTemplate` | create product template linked to active raw material and packaging types with required price | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.product_template.create` | `apps/api/test/catalog-controller.test.ts`, `apps/api/test/catalog-db.integration.test.ts` |
| `PATCH /catalog/product-templates/:id` | `CatalogController.updateProductTemplate` | update product template links/name/price/active | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.product_template.update` | `apps/api/test/catalog-db.integration.test.ts` |
| `PATCH /catalog/product-templates/:id/archive` | `CatalogController.archiveProductTemplate` | set product template `active=false` | `catalog.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `catalog.product_template.archive` | `apps/api/test/catalog-db.integration.test.ts` |
| `GET /clients` | `ClientsController.listClients` | list/search mutable client master data by name or normalized phone | `client.read` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/clients-controller.test.ts`, `apps/api/test/clients-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /clients` | `ClientsController.createClient` | create client with unique normalized phone | `client.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `client.create` | `apps/api/test/clients-controller.test.ts`, `apps/api/test/clients-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `PATCH /clients/:clientId` | `ClientsController.updateClient` | update current client card and recompute normalized phone if phone changes | `client.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `client.update` | `apps/api/test/clients-controller.test.ts`, `apps/api/test/clients-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /production/options` | `ProductionController.options` | read active raw material, packaging and priced product template options for production forms | `production.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/production-db.integration.test.ts` |
| `GET /production/summary` | `ProductionController.summary` | production home summary: ready product units from workshop balances, raw/packaging aggregate balances | `production.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/production-controller.test.ts`, `apps/api/test/production-db.integration.test.ts` |
| `GET /production/raw-material-balances` | `ProductionController.rawMaterialBalances` | list current raw material balances in workshop | `production.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/production-db.integration.test.ts` |
| `POST /production/raw-material-intakes` | `ProductionController.createRawMaterialIntake` | create raw material intake and increment raw balance | `production.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `production.raw_material_intake.create` | `apps/api/test/production-controller.test.ts`, `apps/api/test/production-db.integration.test.ts` |
| `GET /production/packaging-balances` | `ProductionController.packagingBalances` | list current packaging balances in workshop | `production.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/production-db.integration.test.ts` |
| `POST /production/packaging-intakes` | `ProductionController.createPackagingIntake` | create packaging intake and increment packaging balance | `production.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `production.packaging_intake.create` | `apps/api/test/production-db.integration.test.ts` |
| `GET /production/product-batches` | `ProductionController.productBatches` | list product batch release history for production screen | `production.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/production-db.integration.test.ts` |
| `POST /production/product-batches` | `ProductionController.createProductBatch` | release product batch, decrement raw/packaging balances, snapshot template fields | `production.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `production.product_batch.create` | `apps/api/test/production-controller.test.ts`, `apps/api/test/production-db.integration.test.ts` |
| `GET /production/workshop-product-balances` | `ProductionController.workshopProductBalances` | list non-zero finished product balances currently available in workshop | `production.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/production-db.integration.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /production/transfer-options` | `ProductionController.transferOptions` | read active distributors and available workshop product balances for transfer form | `production.manage` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/production-db.integration.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /production/product-transfers` | `ProductionController.createProductTransfer` | transfer finished product from workshop to distributor with conditional workshop decrement and distributor priced stock increment at base batch price | `production.manage` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `production.product_transfer.create`, typed `product_transfer` with price snapshot | `apps/api/test/production-controller.test.ts`, `apps/api/test/production-db.integration.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /distributor/inventory` | `DistributorController.inventory` | read non-zero distributor priced stock rows with effective-price total stock value summary | `distributor.stock.read` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/distributor-controller.test.ts`, `apps/api/test/distributor-inventory-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /distributor/sale-options` | `DistributorController.saleOptions` | read active distributor priced stock rows available for distributor sale | `distributor.sale.create` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/distributor-controller.test.ts`, `apps/api/test/distributor-sales-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /distributor/cash-balances` | `DistributorController.cashBalances` | read distributor cash balance projection, returning zero for active distributors without cash row and `active=false` for archived read-only cash rows | `distributor.cash.read` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/distributor-controller.test.ts`, `apps/api/test/distributor-sales-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /distributor/sales` | `DistributorController.createSale` | create distributor sale by priced `distributorProductBalanceId`, decrement stock and increment cash only for cash payment | `distributor.sale.create` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `distributor.sale.create`, typed `distributor_sale` with base/effective/discount price snapshot | `apps/api/test/distributor-controller.test.ts`, `apps/api/test/distributor-sales-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /distributor/sales/history` | `DistributorController.salesHistory` | read paginated distributor sales history with search, status filter, cancellation status and separate sale/cancel actors | `distributor.sale.create` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/distributor-controller.test.ts`, `apps/api/test/distributor-sales-db.integration.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /distributor/sales/:saleId/cancel` | `DistributorController.cancelSale` | cancel distributor sale with required reason; restore product to original priced stock row and decrement aggregate distributor cash only if current cash balance is enough | `distributor.sale.cancel` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `distributor.sale.cancel`, typed `distributor_sale_cancellation` | `apps/api/test/distributor-controller.test.ts`, `apps/api/test/distributor-sales-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /distributor/cash-withdrawals` | `DistributorController.createCashWithdrawal` | withdraw cash from active distributor with conditional cash decrement; comment is optional | `cash.withdraw` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `distributor.cash.withdraw`, typed `distributor_cash_withdrawal` | `apps/api/test/distributor-controller.test.ts`, `apps/api/test/distributor-sales-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /distributor/discounts` | `DistributorController.assignDiscount` | split distributor priced stock into a lower-price row with conditional source decrement and target upsert/increment | `discount.assign` через `RequirePermission` + `PolicyGuard` | `operation` + `audit_log` с `distributor.discount.assign`, typed `product_discount_assignment` | `apps/api/test/distributor-controller.test.ts`, `apps/api/test/distributor-sales-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /courier/load-options` | `CourierController.loadOptions` | read active distributor priced stock rows available for courier self-load | `courier.stock.load` через `RequirePermission` + `PolicyGuard` | нет | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /courier/product-balances` | `CourierController.productBalances` | read non-zero courier priced stock balances; courier scope is own user, director/commercial/admin scope is all couriers | `courier.stock.read` через `RequirePermission` + `PolicyGuard` и service-level scope | нет | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /courier/loads` | `CourierController.createLoad` | load product from distributor priced stock to courier priced stock balance by `distributorProductBalanceId`; courier can load only self, admin must pass explicit courier | `courier.stock.load` через `RequirePermission` + `PolicyGuard` и service-level target validation | `operation` + `audit_log` с `courier.stock.load.create`, typed `courier_load` with price snapshot | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /courier/sale-options` | `CourierController.saleOptions` | read non-zero courier priced stock balances available for courier sale; courier scope is own user, admin scope is all couriers | `courier.sale.create` через `RequirePermission` + `PolicyGuard` и service-level scope | нет | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /courier/cash-balances` | `CourierController.cashBalances` | read courier cash balance projection; courier scope is own user, director/commercial/admin scope is all couriers, including zero rows for couriers without cash operations | `courier.cash.read` через `RequirePermission` + `PolicyGuard` и service-level scope | нет | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /courier/sales` | `CourierController.createSale` | create courier sale by priced `courierProductBalanceId`, decrement courier stock and increment courier cash only for cash payment | `courier.sale.create` через `RequirePermission` + `PolicyGuard` и service-level target validation | `operation` + `audit_log` с `courier.sale.create`, typed `courier_sale` with base/effective/discount price snapshot | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /courier/sales/history` | `CourierController.salesHistory` | read paginated courier sales history with search, status filter and cancellation status; courier scope is own sales, admin backend scope is all courier sales | `courier.sale.create` через `RequirePermission` + `PolicyGuard` и service-level scope | нет | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /courier/sales/:saleId/cancel` | `CourierController.cancelSale` | cancel courier sale with required reason; courier can cancel only own sale, product returns to original priced courier row and aggregate courier cash decrements only if enough | `courier.sale.cancel` через `RequirePermission` + `PolicyGuard` и service-level owner validation | `operation` + `audit_log` с `courier.sale.cancel`, typed `courier_sale_cancellation` | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /courier/unload-options` | `CourierController.unloadOptions` | read active distributors, own courier product balances and own courier cash balance for courier unload self-flow | `courier.unload.create` через `RequirePermission` + `PolicyGuard` и service-level courier self-flow scope | нет | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /courier/unloads` | `CourierController.createUnload` | unload several courier priced stock rows and/or courier cash to selected active distributor; returned product keeps the same `unitPriceCents`; courier self-flow, admin backend-flow with explicit `courierUserId` | `courier.unload.create` через `RequirePermission` + `PolicyGuard` и service-level target validation | `operation` + `audit_log` с `courier.unload.create`, typed `courier_unload` + `courier_unload_item` with price snapshot | `apps/api/test/courier-controller.test.ts`, `apps/api/test/courier-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `GET /notifications` | `NotificationsController.listNotifications` | read production notifications with optional status filter; new items sorted first | `notification.read` через `RequirePermission` + `PolicyGuard` и service-level role guard | нет | `apps/api/test/notifications-controller.test.ts`, `apps/api/test/notifications-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `POST /notifications` | `NotificationsController.createNotification` | create free text task for production; creator is actor, recipient is production | `notification.create` через `RequirePermission` + `PolicyGuard` и service-level role guard | `operation` + `audit_log` с `production.notification.create`, typed `production_notification` | `apps/api/test/notifications-controller.test.ts`, `apps/api/test/notifications-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |
| `PATCH /notifications/:id/complete` | `NotificationsController.completeNotification` | mark new production notification as completed; repeated completion is rejected | `notification.complete` через `RequirePermission` + `PolicyGuard` и service-level role guard | `operation` + `audit_log` с `production.notification.complete`, typed `production_notification` | `apps/api/test/notifications-controller.test.ts`, `apps/api/test/notifications-db.integration.test.ts`, `apps/api/test/policy.test.ts`, `apps/web/app/page.test.tsx` |

## 3. Минимальный формат будущей строки

Не описывать вымышленные endpoints до появления handler в коде. Новые строки добавлять только по фактической реализации route и тестов.

| Route | Handler | Domain operation | Rights | History | Tests |
|---|---|---|---|---|---|
